export default function HomePage() {
  return (
    <main className="px-[var(--pad-x-sp)] py-16 md:px-[var(--pad-x)]">
      <h1 className="text-fg text-[length:var(--text-display-sp)] font-extrabold tracking-tight md:text-[length:var(--text-display)]">
        MasKOFF
      </h1>
      <p className="text-fg-muted mt-1 text-[length:var(--text-sub)]">セットアップ完了test</p>
    </main>
  );
}
